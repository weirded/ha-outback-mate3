# Test dependencies for both the integration (via pytest-homeassistant-custom-component)
# and the add-on (which needs aiohttp + pytest-asyncio).
pytest>=8.0
pytest-asyncio>=0.23
pytest-homeassistant-custom-component>=0.13.186
aiohttp>=3.11
