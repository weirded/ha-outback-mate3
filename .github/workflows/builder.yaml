name: Builder

on:
  push:
    branches:
      - main
    paths:
      - outback_mate3_addon/**
      - .github/workflows/builder.yaml
  pull_request:
    paths:
      - outback_mate3_addon/**
      - .github/workflows/builder.yaml
  workflow_dispatch:

concurrency:
  group: builder-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build ${{ matrix.arch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch: [aarch64, amd64, armhf, armv7, i386]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sync bundled integration into add-on dir
        run: ./scripts/sync-bundled-integration.sh

      - name: Build add-on (${{ matrix.arch }})
        uses: home-assistant/builder@2025.03.0
        with:
          # --image supplies a tag template; without it the builder constructs
          # an invalid `/:<version>` tag and aborts. We don't set `image:` in
          # config.yaml because that would make local Supervisor installs pull
          # from this registry instead of building locally.
          args: |
            --test \
            --${{ matrix.arch }} \
            --target outback_mate3_addon \
            --image ghcr.io/weirded/{arch}-addon-outback-mate3 \
            --addon
